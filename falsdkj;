oldPosition = position
        if direction == 'L':
            position -= number
        elif direction == 'R':
            position += number
        else:
            print(f"Invalid Direction: {direction}")

        if number % 100 == 0:
            password += number / 100
       
        elif position % 100 != position:
            password += 1
        position = position % 100